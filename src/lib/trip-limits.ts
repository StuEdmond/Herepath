/** Limits for saved trips. In a file of their own so the trip page and the server can both use them. */
export const MAX_SAVED_TRIPS = 10;
export const TRIP_NAME_MAX_LENGTH = 80;
export const DAY_LENGTH_OPTIONS = [100, 150, 200, 250] as const;
